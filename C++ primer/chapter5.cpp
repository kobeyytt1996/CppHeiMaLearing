#include <iostream>
#include <vector>
using namespace std;

void printNum(unsigned int ui) {
	string s(ui, '0');
	while (s.size() < ui) {

	}
}

int main() {
	double d = 0.0, d1 = 0.1;
	vector<int> vi1;
	for (decltype(vi1.size()) i = 0, j = vi1.size(); i != j; ++i) {

	}
	char ch = '\0';
	while (!(cin >> ch).eof()) {
		cout << 1 << endl;
	}

	return 0;
}